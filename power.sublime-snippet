<snippet>
	<content><![CDATA[
ll power(ll a, ll b)
{  
    ll result = 1;
    while (b > 0) {
        if (b & 1ll) result = result * a % MOD ;
        b = b >> 1ll;
        a = a * a % MOD;
    }
    return result;
}$1
]]></content>
<tabTrigger>power</tabTrigger>
</snippet>
